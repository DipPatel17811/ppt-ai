"""Raw OOXML writers for PowerPoint-only features (transitions,
animations, morph) that python-pptx does not expose.

The rest of the project never touches these internals directly; the
``compiler`` module is the only gateway.
"""
