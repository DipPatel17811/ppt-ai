"""AI package: semantic planning + strict-JSON generation.

The AI's *only* job is to produce a validated Presentation AST.  Everything
after it (validation, templates, layout, rendering, compiling) is
deterministic code.
"""
